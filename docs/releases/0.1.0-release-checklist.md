# JLCEDA2KICAD 0.1.0 release checklist

Date: 2026-07-15 (Asia/Shanghai)

This record separates automated local evidence from live GitHub and KiCad GUI
checks. An unchecked item is not a pass.

## Candidate identity and environment

- Verified implementation commit: `4fa18e272c6c965e6477fef1cc6dd83cf6bb6232`.
- Local `main` and `codex/github-pcm-release` both pointed to that commit during
  the final automated gate.
- Operating system: Microsoft Windows 11 Pro, version `10.0.26200`, build `26200`.
- Test interpreter: Python `3.11.5`.
- Installed KiCad command-line versions: `9.0.6` and `10.0.4`.
- Release version is `0.1.0` in source, metadata, and changelog.

## Automated source and release-policy gates

- [x] `.venv/Scripts/ruff.exe check .` — exit 0, all checks passed.
- [x] `.venv/Scripts/python.exe -m mypy src scripts` — exit 0, 38 source files
  checked with no issues.
- [x] `QT_QPA_PLATFORM=offscreen python -m pytest --cov=jlceda2kicad
  --cov-report=term-missing --cov-report=xml` — exit 0; `325 passed, 1 skipped`;
  total coverage `84%`.
- [x] The skipped test was the opt-in C2040 network conversion, which requires
  `RUN_LIVE_LCSC_TESTS=1`; it is not counted as a live-query pass.
- [x] A temporary local `v0.1.0` tag passed `scripts/check_release.py --tag
  v0.1.0` against local `main`; the temporary tag was then deleted.
- [x] Direct-file invocation tests passed for `check_release.py`,
  `publish_release.py`, and `build_repository_site.py`.

## Offline PCM artifact

- Archive: `JLCEDA2KICAD-0.1.0.zip`.
- SHA-256: `9468a0b972ee26be77c38caebe4b5bdee4503f5793c1bcbfa9e0e49b8efb35c2`.
- Download size: `247,964,777` bytes.
- Install size: `675,042,494` bytes.
- ZIP members: `4,224`; sorted manifest lines: `4,224`.
- [x] SHA-256 sidecar and sorted manifest match the archive.
- [x] Two independent package builds were byte-identical.
- [x] `python -m kipy.packaging validate` exited 0 with `Validation passed`.
  It emitted the expected warning that the comment-only `plugins/requirements.txt`
  does not list `kicad-python`; the runtime is intentionally bundled under
  `plugins/vendor` instead of being downloaded at install time.
- [x] The exact ZIP passed offline runtime smoke tests with both KiCad 9 and
  KiCad 10 Python. `easyeda2kicad --help` exited 0 and all inspected imports,
  including PySide6, kipy, pynng, and easyeda2kicad, resolved from
  `plugins/vendor` while package indexes and external site-packages were disabled.

## Local dual-schema PCM repository

Fixed publication input: `2026-07-15T08:00:00Z`, base URL
`http://127.0.0.1:8765`.

| File | SHA-256 |
| --- | --- |
| `index.html` | `1382cac7859e75d6b7bf10129ec6b8c84da886e80d5293df07d3c8e4e181dd64` |
| `resources.zip` | `a016658f5e3f618ab745818e7bf9205040227cf7364c2c8b1b0c9f58016cfebf` |
| `v1/packages.json` | `44231aa5c1f8212b262ed58d69a4d341eaa4dfb4077748b515354d3e8d99aa75` |
| `v1/repository.json` | `be0edb2fe21e4f62e4d2113454259759ece1feeefcf0bb3b5c525e74477a0c61` |
| `v2/packages.json` | `5b01be93a42f16e95e93e151c517a7c9318cd7b281ca55bd7f17ced685962f88` |
| `v2/repository.json` | `a9a5c706ff24d6442eab1dfe2ca552234c627e25ea2e41308040735e039bb62c` |

- [x] `/`, both repository descriptors, both package lists, `resources.zip`,
  and the PCM ZIP returned HTTP 200 from the loopback server.
- [x] Descriptor hashes matched the corresponding package/resource files.
- [x] Both package lists referenced the exact archive SHA-256 and sizes above.
- [x] A second complete site build contained the same seven relative paths and
  identical bytes for every file.

## Live publication and GUI checks

- [ ] GitHub Release workflow run is green for preflight, Windows/Ubuntu tests,
  package, Release publication, Pages build, and Pages deployment.
- [ ] Public Release and the ZIP, SHA-256, and manifest asset URLs return HTTP 200.
- [ ] `https://hkrpt.github.io/JLCEDA2KICAD/v1/repository.json` is live and its
  complete public hash chain is verified.
- [ ] `https://hkrpt.github.io/JLCEDA2KICAD/v2/repository.json` is live and its
  complete public hash chain is verified.
- [ ] KiCad 9.0.6 installs from the v1 repository in isolated configuration and
  documents roots, launches the plugin, and uninstalls it.
- [ ] KiCad 10.0.4 installs from the v2 repository in isolated configuration and
  documents roots, launches this exact candidate, and uninstalls it.
- [ ] KiCad 10.0.4 installs this exact ZIP through “Install from File”, launches
  it, and uninstalls it.
- [ ] The local two-version update fixture is exercised in the KiCad GUI.
- [ ] A real public update is verified; not applicable until a second public
  version exists.

No GitHub tag, Release, Pages deployment, or public asset was created during
this local preflight. The unchecked live items above remain unverified.
